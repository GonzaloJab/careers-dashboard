server {
    # Allow viewing via IP before DNS is ready
    listen 80 default_server;
    server_name careers.laminarpay.com 49.12.73.66 _;

    # API — proxy to FastAPI
    location /api/ {
        proxy_pass         http://127.0.0.1:8000/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        client_max_body_size 10M;
    }

    # Frontend — React build output
    location / {
        root  /opt/careers/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}

# SSL block added automatically by certbot — do not edit manually
